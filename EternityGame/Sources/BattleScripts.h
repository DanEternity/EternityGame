#pragma once
#include<Types.h>
#include<Battle.h>
#include<Adventure_base.h>

ScriptResult CreateTestField(
	tBaseAdventure * pAdventure,
	tBattle * pBattle,
	int attribute1,
	int attribute2,
	int * attributeArray);
